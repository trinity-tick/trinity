[pytest]
# Trinity v8.0 E2E Test Configuration
asyncio_mode = auto
testpaths = trinity/tests
timeout = 60
log_level = INFO
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests as integration tests requiring live server
    e2e: marks tests as end-to-end multi-agent scenarios
