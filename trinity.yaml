# Trinity Configuration — Local (non-Docker)
# ============================================================
# Auto-generated: 2026-08-07
# Purpose:    Local development configuration with PostgreSQL backend
# Migration:  SQLite → PostgreSQL (29 memories migrated)

storage:
  db_type: postgresql
  path: ./data
  pg_host: 127.0.0.1
  pg_port: 5432
  pg_db: trinity
  pg_user: postgres
  pg_password: postgres
  pg_min_conn: 1
  pg_max_conn: 10

server:
  mcp:
    transport: stdio
    port: 8000
    host: 0.0.0.0
  api:
    port: 8001
    host: 0.0.0.0

guardian:
  enabled: true
  tiers: all
  blocking_policy: first_fail

benchmark:
  mock_personas: 3
  mock_top_k: 10
  default_top_k: 5
